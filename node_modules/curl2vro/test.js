const curl2vRO = require('./curl2vRO');

// Example curl command
const curlCommand = `curl https://api.sampleapis.com/coffee/hot`;

// Convert to vRO code
const vroCode = curl2vRO.convertCurlToVRO(curlCommand, false);
console.log(vroCode);